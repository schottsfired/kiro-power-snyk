---
values:
  - ../values.yaml
suite: test deployment (deployment.yaml)
set:
  global:
    polaris:
      cloudProvider: aws
      tags:
        owner: helmunit
        service: infra-app-template
templates:
  - templates/deployment.yaml
#tests:
#  - it: should do this thing
#    set: 
#    asserts:
